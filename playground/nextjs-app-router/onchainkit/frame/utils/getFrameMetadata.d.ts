import type { FrameMetadataResponse, FrameMetadataType } from '../types';
/**
 * This function generates the metadata for a Farcaster Frame.
 */
export declare const getFrameMetadata: ({ accepts, buttons, image, input, isOpenFrame, postUrl, post_url, refreshPeriod, refresh_period, state, }: FrameMetadataType) => FrameMetadataResponse;
//# sourceMappingURL=getFrameMetadata.d.ts.map