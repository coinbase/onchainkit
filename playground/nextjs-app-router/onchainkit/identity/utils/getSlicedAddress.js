/**
 * Returns the first 6 and last 4 characters of an address.
 */
const getSlicedAddress = address => {
  return `${address.slice(0, 6)}...${address.slice(-4)}`;
};
export { getSlicedAddress };
//# sourceMappingURL=getSlicedAddress.js.map
