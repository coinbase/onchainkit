---
title: Swap components & utilities Types
description: Glossary of Types in Swap components & utilities.
---

# Types [Glossary of Types in Swap components & utilities.]

## `SwapAmountInputReact`

```ts
type SwapAmountInputReact = {
  amount?: string; // Optional token amount (overrides internal implementation)
  label: string; // Descriptive label for the input field
  setAmount: (amount: string) => void; // Optional callback function when the amount changes (overrides internal implementation)
  token?: Token; // Selected token
  type: 'to' | 'from'; // Identifies if component is for toToken or fromToken
};
```

## `SwapReact`

```ts
export type SwapReact = {
  account: Account // Ethereum account
  children: ReactNode // Children components to render
};
```
